name: Deploy to Jamsocket

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Jamsocket CLI
        run: npm install -g jamsocket

      - name: Authenticate Jamsocket
        run: npx jamsocket login --token $JAMSOCKET_API_KEY
        env:
          JAMSOCKET_API_KEY: ${{ secrets.JAMSOCKET_API_KEY }}

      - name: Push to Jamsocket
        run: npx jamsocket push trekker-md -f Dockerfile
