schemaVersion: 1.0
name: server1
type: service
projectType: choreo

runtime: nodejs21

build:
  command: yarn --frozen-lockfile install && yarn build
  output: .

run:
  command: npm run dev
  containerPort: 8000

env:
  - name: NODE_ENV
    value: development
